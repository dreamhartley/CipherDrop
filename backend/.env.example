# 服务器端口
PORT=3001

# 每个会话的最大存储空间 (1GB，设置为-1表示无限存储)
MAX_SESSION_STORAGE_BYTES=1073741824

# 最大活跃会话数量 (设置为-1表示无限制)
MAX_ACTIVE_SESSIONS=5

# 允许的前端域名 (用逗号分隔，留空表示只允许同源访问)
#ALLOWED_ORIGINS=http://localhost:5173,http://localhost,http://127.0.0.1:5173,http://127.0.0.1
